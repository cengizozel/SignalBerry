<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Top bar (unchanged) -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:paddingEnd="12dp"
        android:background="@color/colorBackground">

        <!-- generous hit box: full bar height, flush to the screen edge,
             with the icon centered inside it -->
        <ImageButton
            android:id="@+id/btn_back"
            android:layout_width="64dp" android:layout_height="match_parent"
            android:scaleType="center"
            android:layout_centerVertical="true"
            android:background="@android:color/transparent"
            android:src="@android:drawable/ic_media_previous"
            android:contentDescription="Back" />

        <LinearLayout
            android:id="@+id/title_container"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:orientation="horizontal"
            android:gravity="center_vertical">
            <ImageView
                android:id="@+id/iv_chat_avatar"
                android:layout_width="30dp" android:layout_height="30dp"
                android:layout_marginEnd="8dp"
                android:contentDescription="Avatar"/>
            <TextView
                android:id="@+id/title_name"
                android:layout_width="wrap_content" android:layout_height="wrap_content"
                android:text="Chat" android:textStyle="bold" android:textSize="18sp"/>
        </LinearLayout>

        <ImageButton
            android:id="@+id/btn_chat_search"
            android:layout_width="56dp" android:layout_height="match_parent"
            android:scaleType="center"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:background="@android:color/transparent"
            android:src="@android:drawable/ic_menu_search"
            android:contentDescription="Search messages"/>
    </RelativeLayout>

    <!-- In-chat search (toggled from the title bar) -->
    <LinearLayout
        android:id="@+id/chat_search_bar"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@color/colorSurface"
        android:paddingStart="8dp" android:paddingEnd="4dp"
        android:visibility="gone">

        <EditText
            android:id="@+id/chat_search_input"
            android:layout_width="0dp" android:layout_height="wrap_content"
            android:layout_weight="1"
            android:hint="Search messages"
            android:singleLine="true"
            android:imeOptions="actionSearch"
            android:textSize="14sp"
            android:background="@android:color/transparent"/>

        <TextView
            android:id="@+id/chat_search_count"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:textSize="12sp"
            android:paddingStart="4dp" android:paddingEnd="4dp"
            android:textColor="@color/colorTextMuted"/>

        <ImageButton
            android:id="@+id/btn_search_up"
            android:layout_width="44dp" android:layout_height="match_parent"
            android:scaleType="center"
            android:background="@android:color/transparent"
            android:src="@android:drawable/arrow_up_float"
            android:contentDescription="Previous match"/>

        <ImageButton
            android:id="@+id/btn_search_down"
            android:layout_width="44dp" android:layout_height="match_parent"
            android:scaleType="center"
            android:background="@android:color/transparent"
            android:src="@android:drawable/arrow_down_float"
            android:contentDescription="Next match"/>

        <ImageButton
            android:id="@+id/btn_search_close"
            android:layout_width="44dp" android:layout_height="match_parent"
            android:scaleType="center"
            android:background="@android:color/transparent"
            android:src="@android:drawable/ic_menu_close_clear_cancel"
            android:contentDescription="Close search"/>
    </LinearLayout>

    <!-- Selection bar (shown in multi-select mode) -->
    <LinearLayout
        android:id="@+id/selection_bar"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="#1976D2"
        android:paddingStart="4dp" android:paddingEnd="4dp"
        android:visibility="gone">

        <ImageButton
            android:id="@+id/btn_cancel_selection"
            android:layout_width="48dp" android:layout_height="match_parent"
            android:scaleType="center"
            android:background="@android:color/transparent"
            android:src="@android:drawable/ic_menu_close_clear_cancel"
            android:tint="#FFFFFF"
            android:contentDescription="Cancel selection"/>

        <TextView
            android:id="@+id/tv_selection_count"
            android:layout_width="0dp" android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:textStyle="bold"
            android:layout_marginStart="8dp"/>

        <ImageButton
            android:id="@+id/btn_delete_selected"
            android:layout_width="48dp" android:layout_height="match_parent"
            android:scaleType="center"
            android:background="@android:color/transparent"
            android:src="@android:drawable/ic_menu_delete"
            android:tint="#FFFFFF"
            android:contentDescription="Delete selected"/>
    </LinearLayout>

    <!-- Messages list + jump-to-bottom overlay -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/chat_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="8dp"
            android:clipToPadding="false" />

        <FrameLayout
            android:id="@+id/btn_jump_bottom"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:layout_gravity="bottom|end"
            android:layout_margin="12dp"
            android:background="@drawable/bg_jump_bottom"
            android:visibility="gone">

            <ImageView
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_gravity="center"
                android:src="@android:drawable/arrow_down_float"
                android:contentDescription="Jump to latest"/>

            <TextView
                android:id="@+id/tv_jump_unread"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="top|end"
                android:background="@drawable/bg_badge"
                android:textColor="#FFFFFF"
                android:textSize="10sp"
                android:paddingLeft="5dp" android:paddingRight="5dp"
                android:visibility="gone"/>
        </FrameLayout>
    </FrameLayout>

    <!-- Typing indicator (shown when peer is typing) -->
    <TextView
        android:id="@+id/tv_typing"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="4dp"
        android:paddingBottom="2dp"
        android:textSize="12sp"
        android:textStyle="italic"
        android:textColor="@color/colorTextMuted"
        android:visibility="gone"/>

    <!-- Reply preview (shown when user long-presses a message) -->
    <LinearLayout
        android:id="@+id/reply_preview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="12dp" android:paddingEnd="4dp"
        android:paddingTop="6dp" android:paddingBottom="6dp"
        android:background="@color/colorSurface"
        android:visibility="gone">

        <View
            android:layout_width="3dp"
            android:layout_height="32dp"
            android:background="#2196F3"
            android:layout_marginEnd="8dp"/>

        <TextView
            android:id="@+id/tv_reply_preview"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:maxLines="1"
            android:ellipsize="end"
            android:textSize="13sp"
            android:textColor="@color/colorTextSecondary"/>

        <ImageButton
            android:id="@+id/btn_cancel_reply"
            android:layout_width="48dp"
            android:layout_height="44dp"
            android:scaleType="center"
            android:src="@android:drawable/ic_menu_close_clear_cancel"
            android:background="@android:color/transparent"
            android:contentDescription="Cancel reply"/>
    </LinearLayout>

    <!-- Debug log (toggle in Settings) -->
    <LinearLayout
        android:id="@+id/debug_panel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_margin="8dp"
        android:padding="8dp"
        android:background="@drawable/bg_debug_panel"
        android:visibility="gone">
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">
            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="DEBUG LOG"
                android:textSize="10sp"
                android:letterSpacing="0.1"
                android:textStyle="bold"
                android:textColor="@color/colorTextMuted"/>
            <TextView
                android:id="@+id/btn_copy_log"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingLeft="10dp" android:paddingRight="10dp"
                android:paddingTop="4dp" android:paddingBottom="4dp"
                android:text="COPY"
                android:textSize="11sp"
                android:textStyle="bold"
                android:textColor="#2196F3"/>
            <TextView
                android:id="@+id/btn_clear_log"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingLeft="10dp" android:paddingRight="10dp"
                android:paddingTop="4dp" android:paddingBottom="4dp"
                android:text="CLEAR"
                android:textSize="11sp"
                android:textStyle="bold"
                android:textColor="#2196F3"/>
        </LinearLayout>
        <ScrollView
            android:id="@+id/debug_scroll"
            android:layout_width="match_parent"
            android:layout_height="120dp">
            <TextView
                android:id="@+id/debug_log"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="@color/colorTextSecondary"
                android:textSize="10sp"
                android:fontFamily="monospace"
                android:paddingTop="4dp"/>
        </ScrollView>
    </LinearLayout>

    <!-- Composer (unchanged) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:orientation="horizontal"
        android:paddingStart="8dp" android:paddingEnd="8dp"
        android:gravity="center_vertical"
        android:background="@color/colorBackground">

        <ImageButton
            android:id="@+id/btn_attach"
            android:layout_width="44dp" android:layout_height="match_parent"
            android:scaleType="center"
            android:background="@android:color/transparent"
            android:src="@android:drawable/ic_menu_gallery"
            android:contentDescription="Attach" />

        <TextView
            android:id="@+id/btn_emoji"
            android:layout_width="40dp" android:layout_height="match_parent"
            android:gravity="center"
            android:text="😊"
            android:textSize="20sp"
            android:contentDescription="Emoji" />

        <EditText
            android:id="@+id/input_message"
            android:layout_width="0dp" android:layout_height="wrap_content"
            android:layout_weight="1"
            android:hint="Message"
            android:inputType="textCapSentences|textMultiLine"
            android:maxLines="4"
            android:imeOptions="actionSend"
            android:textColor="#222222"
            android:textColorHint="@color/colorTextMuted"
            android:background="@android:drawable/edit_text"
            android:padding="10dp"/>

        <ImageButton
            android:id="@+id/btn_mic"
            android:layout_width="44dp" android:layout_height="match_parent"
            android:scaleType="center"
            android:background="@android:color/transparent"
            android:src="@android:drawable/ic_btn_speak_now"
            android:contentDescription="Record voice message" />

        <ImageButton
            android:id="@+id/btn_send"
            android:layout_width="56dp" android:layout_height="match_parent"
            android:scaleType="center"
            android:background="@android:color/transparent"
            android:src="@android:drawable/ic_menu_send"
            android:contentDescription="Send" />
    </LinearLayout>
</LinearLayout>
