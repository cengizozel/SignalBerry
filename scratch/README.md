# scratch

A personal, gitignored place to keep loose files while working: throwaway
scripts, notes, sample data, query dumps, whatever. Everything in here is
ignored by git except this README, so nothing you drop in gets committed by
accident and you do not have to stash it somewhere unrelated outside the repo.

Not shared, not built, not deployed. Safe to delete anything here at any time.
